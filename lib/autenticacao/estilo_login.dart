import 'package:flutter/material.dart';

final loginDicaTextoEstilo = TextStyle(
  color: Colors.black,
);

final loginLabelEstilo = TextStyle(
  color: Colors.black,
);

final loginBoxDecorationEstilo = BoxDecoration(
  color: Colors.grey.shade200,
  borderRadius: BorderRadius.circular(10.0),
  boxShadow: [
    BoxShadow(
      color: Colors.grey.shade300,
      blurRadius: 6.0,
      offset: Offset(0, 2),
    ),
  ],
);
